{
  "name": "simple-axios-app",
  "version": "1.0.0",
  "description": "A simple web app using axios",
  "main": "index.html",
  "scripts": {
    "start": "npx http-server -p 8080",
    "test": "echo \"Running tests...\" && exit 0",
    "lint": "echo \"Linting code...\" && exit 0",
    "format:check": "echo \"Checking format...\" && exit 0",
    "build": "echo \"Build complete\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.6.7"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
